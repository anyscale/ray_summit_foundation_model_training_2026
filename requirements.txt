# Foundation Model Distributed Training with Ray — pinned dependencies
#
# Tested on the Anyscale image `anyscale/ray:2.55.1-py311-cu128`
# (Ray 2.55.1, Python 3.11, CUDA 12.8) on a 4x NVIDIA T4 cluster.
#
# Install with plain pip (NO --index-url): the default PyPI torch wheel for
# 2.9.x is already a CUDA 12.8 build, and on an Anyscale workspace a plain
# `pip install` registers the package on ALL cluster nodes. A custom
# --index-url is NOT propagated across the cluster.
#
#   pip install -r requirements.txt
#
# ray is provided by the base image (2.55.1); listed here for completeness.

torch==2.9.1
torchvision==0.24.1
transformers==4.48.0
datasets==2.21.0
deepspeed==0.18.9
accelerate==1.3.0
torchmetrics==1.6.1
hf_transfer==0.1.8  # faster HuggingFace downloads (HF_HUB_ENABLE_HF_TRANSFER=1)
# ray==2.55.1  # from base image
